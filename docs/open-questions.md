# 미정 사항 체크리스트

**아직 안 정해진 것만** 위에 둡니다. 정해지면 아래 §결정 기록으로 한 줄로 내리고, 반영 위치를 적습니다. 전부 반영되면 이 파일을 지웁니다.

> 권위는 `docs/테크스펙.md`입니다. 여기서 정한 건 테크스펙에 먼저 반영하고, 그 다음 `CLAUDE.md`에 옮깁니다.

## A. 컨벤션

- [ ] `.editorconfig` + pre-commit 훅 도입 여부
- [x] ~~Ruff가 아직 설치·설정되지 않음~~ — 09/21 `requirements-dev.in`에 추가하고 `backend/ruff.toml` 생성. 줄 길이는 `backend/CLAUDE.md` §코드 스타일의 100자를 따릅니다. 절차는 `backend/README.md` §린트와 포맷
- [ ] 에러 응답의 세부 필드명 — `detail` vs `details` vs 생략

## B. 구조 · 인프라

- [ ] ONNX 모델 배포 방식: git-lfs vs S3/CDN vs `public/` 직접 포함
- [ ] 로컬 개발 환경: Docker Compose 통일 vs 각자 로컬 실행
- [ ] CI에서 막을 것: 린트 / 타입체크 / 테스트 / 빌드 중 어디까지 — **현재 린트·포맷·테스트로 운영 중**(`.github/workflows/ci.yml`). 머지 차단은 `develop-ci` 룰셋으로 켰다가, 룰셋 도입 전에 열린 PR이 검사 대기 상태로 막혀 임시 해제했습니다. 타입체크·빌드를 넣을지는 미정
- [ ] **죽은 참조·깨진 링크 CI 검사 도입 여부** — 문서의 `§제목`·경로 참조가 실제로 존재하는지 검사. 블랭킷 경로 검사는 오탐이 많아(`models.py` 같은 관례 표기까지 잡힘) 표적 검사로 짜야 합니다. 새 워크플로라 BE 리드 확인 필요
- [ ] `backend/.importlinter` — 채울지 지울지 (현재 0바이트. 빈 설정 파일이 제일 나쁨)
- [ ] **`backend/domains/agents/CLAUDE.md` §프롬프트를 새 구조에 맞게 고치기** (AI 회의 답변으로 방향 확정, **코드 반영 대기 중**) — 현재 문서는 "코드 문자열에 하드코딩하지 않고 최상위 `prompts/*.md`에 둔다 / 파일명은 `agent1_evidence.md` 등 4개"입니다. 새 구조는 **`prompts/<영역>/` 중첩, 프롬프트 본문은 `.md`, 변수 삽입·조립 로직은 `.py`로 분리**하며, 하위 전체를 한 확장자로 고정하지 않습니다. 지금 저장소의 `prompts/verification/critic.py`는 본문과 조립이 한 파일에 섞여 있어 아직 새 구조가 아닙니다 — **AI팀 PR로 분리가 끝난 뒤 `[fix]`로 문서를 맞춥니다.** 먼저 고치면 문서가 또 현실과 어긋납니다
- [ ] **Celery worker가 죽었을 때 자동 복구할지** (09/19 논의, 결론 미정) — 하는 쪽이 낫지만 **워커가 중복 실행될 수 있다**는 단점이 있습니다(같은 작업이 두 번 돌 수 있음). Celery에 관련 옵션이 있고 **현재는 비활성화**해 뒀습니다. 켤지, 켠다면 중복을 무엇으로 막을지(작업 멱등성 / 락) 미정
- [ ] **배포 실행 방식** (NFR-13) — ① OIDC + SSM 원격 실행(서버에 상시 프로세스 없음) ② self-hosted 러너를 EC2에 설치. `ktc-github-deploy` 역할에 `ssm:SendCommand`가 있는지 확인한 뒤 결정합니다 (`.github/workflows/oidc-test.yml`)
- [ ] **배포 중 중단 허용 범위** (NFR-13) — 현재 설계는 컨테이너 재시작이라 수 초간 API가 끊깁니다. 서버가 1대라 블루/그린은 불가능합니다
- [ ] **`main` 배포(운영 환경) 도입 시점** (NFR-13) — 서버가 1대라 `develop` 배포와 공존할 수 없습니다. 멘토 조언에 따라 우선 `develop`만 붙입니다
- [ ] **컨테이너 로그 장기 보존 여부** — 파일당 10MB·3개로 제한하면 오래된 로그는 사라집니다. 보존이 필요해지면 CloudWatch Logs 등 서버 밖으로 내보내는 방식을 검토합니다 (멘토 09/20: 이번 프로젝트는 디스크 사용량 확인으로 충분)

## C. 스펙 미정 (코드 구조에 영향)

- [ ] **원아별 초대 링크의 발급 주체·형식·만료** (09/22 동의 주체 변경, FR-28) — 누가 링크를 만들어 학부모에게 전달하는지, 링크 형식·만료·재발송 규칙이 비어 있습니다. 확정 전에는 가입·동의 화면을 구현할 수 없습니다
- [ ] **동의 철회 주체** (FR-22, 09/22) — 현재 FR-22는 "교사가 철회 상태로 변경"입니다. 동의를 학부모가 직접 하게 됐으므로 학부모도 철회할 수 있어야 하는지, 아니면 교사 경로만 두는지 미정입니다. 철회는 임베딩 즉시 삭제로 이어지므로 경로를 늘리면 그만큼 검증도 늘어납니다
- [ ] **관찰일지 생성 조건·주기·양식** (09/22 회의, **FE 확정 / AI 회의에 참고용으로 공지 예정**) — 아직 AI팀 합의 전이라 테크스펙에는 반영하지 않았습니다
  - 관찰일지는 매일 쓰는 문서가 아닙니다. **주기는 몇 주에 한 번**이고 **양식이 원마다 다릅니다** (엄태은 전달)
  - 따라서 관찰일지 에이전트를 **에이전트 루프에서 항시 돌리지 않고 특정 조건에서만** 작동시키자는 것 — 그 "특정 조건"의 정의가 미정입니다 (현재 파이프라인 6단계는 알림장과 함께 매번 생성)
  - 작성 시 참고할 컨텍스트: **해당 기간 동안 작성된 알림장 + 관찰일지 양식**. 이때 **원별 관찰일지 양식을 저장하는 기능**이 새로 필요합니다 (FR 추가 대상)
  - **제안(미채택)**: 관찰일지가 텍스트가 아니라 블록 양식이라면 출력도 텍스트면 안 됩니다 — AI가 양식을 보고 블록 예시를 하나 만들어 교사 평가를 받은 뒤 전체 제작. 급하지 않으므로 **현재는 텍스트로 생성**합니다
- [ ] **음성메모 사용을 유도하는 튜토리얼 페이지** (09/22) — 교사가 음성 메시지를 적극적으로 쓰지 않을 가능성이 있습니다. 급하지 않아 **미룹니다**
- [ ] LLM에 보내는 프레임 수 상한 — 아이 1명·하루 기준 3~5장 권장, 확정값 미정 (H-2)
- [ ] FR-19 미승인 초안 보존 기간 — 테크스펙에 `?시간`으로 비어 있음
- [ ] 요구사항 ID 공란(FR-02) 재정렬 시점
- [ ] 보관 기간 안에서 원본 → 저화질(`storage_tier`) 전환 시점 — 같은 객체를 초안 맥락 추출에도 쓰므로, 저화질 전환 후 초안을 재생성(FR-18)하면 열화된 사진으로 맥락을 뽑게 됨
- [ ] 블러 대안 — 추후 ① 현행 유지(LLM 근거에서 제외) ② 교사에게 직접 사진 편집 지시(스티커 삽입 등) ③ 블러 재시도(구현 난이도 높음) 중 검토. **지금은 ①로 갑니다**
- [ ] 미동의 원아가 함께 찍힌 사진의 **학부모 열람·다운로드**(FR-09/10) 노출 여부 — LLM 경로는 막았지만 앨범 노출은 별개 결정
- [ ] **외부 STT 제공자 선정** (09/13 STT 서버 전환). 소음 환경 정확도, 한국어, 위탁 처리 계약·보관 정책(학습 이용 여부), 비용
- [ ] **[rule] `backend/CLAUDE.md` Celery 범위** — 현재 "LLM 호출을 포함하는 파이프라인 4~7단계만". STT가 서버로 오면서 1-B가 새 장시간 작업이 되는데, 동기로 두면 요청 타임아웃이 납니다. 문구를 1-B까지 넓힐지 BE 리드 확인 필요
- [ ] 동의서·위탁계약서에 **음성 처리 외부 위탁** 문구 추가 (09/13 STT 전환)
- [ ] **초안 작성에 쓰이는 "활동계획서"의 정확한 정의** (09/19) — 무엇이 담긴 문서인지, 월간/주간 교육계획(FR-20·21)과 같은 것인지 다른 것인지. **엄태은이 조사해 전파 예정.** 확정되기 전에는 `EvidenceBundle.source_activity_plan_id`에 FK를 걸지 않습니다(데이터 모델 ④)
- [ ] **로그로 무엇을 남기고 무엇을 지울지** (09/19) — `AccessLog`·`DeletionLog`의 기록 대상과 보존 기간. 남기지 않으면 H-4를 못 지키고, 과하게 남기면 그 자체가 개인정보가 됩니다. **한상균이 추가 작업 예정**
- [ ] **새로고침 시 무엇이 보존되어야 하는지** (09/19 논의, 방법 미정) — 업로드·검수 중 새로고침했을 때 로컬(IndexedDB)에 남아야 할 상태의 범위. 데이터 모델 ⑧ `LocalBatch`·`LocalPhoto`와 맞물림
- [ ] **STT·사진 분류 UI를 프론트 어느 화면에 둘지** (09/19) — 추후 프론트 회의에서 결정
- [ ] **AI 모델 미확정** (AI 회의) — 개발 과정에서 후보 모델을 적용하고 관찰 정확도·추론 오류·응답 시간·비용을 비교한 뒤 선정합니다. **계획서에 적힌 후보 모델명은 확정을 뜻하지 않습니다.** 사진 분석·STT 각각 해당
- [ ] **다인원 사진의 공동 귀속 규칙** (AI 회의) — 개인 행동과 공동 장면을 구분하는 근거 규칙이 미확정입니다. `MediaChildLink`의 `child_ids`만으로 모든 행동을 모든 원아에게 귀속하지 않는다는 원칙만 정해졌고, 행동 주체가 확인되지 않으면 개인 행동 기록을 생성하지 않습니다
- [ ] **얼굴 매칭 판정값(M/A/U)의 의미와 임계값** (AI 회의) — 워크플로우에 정의가 없어 구현 전에 명세로 확정해야 합니다
- [ ] **월 12만 원 LLM 예산의 사용량 가정** (AI 회의) — 이 금액은 테크스펙에 없는 숫자입니다. 근거와 산정 기준을 확인한 뒤 어디에 적을지 정합니다
- [ ] 영상·음성 파일이 없는 날의 서비스 동작
- [ ] 영상 중간 프레임을 사진처럼 초안 근거로 쓸지 — 쓴다면 몇 장·선택 기준 (파이프라인 1단계 `frame_extract`)
- [ ] 동의서 법적 문구 확정 (테크스펙 데이터모델 ② "수정 필요")
- [ ] 어린이집 가입 코드(`center_code`) 형식·발급 방식 — 길이, 문자셋, 재발급 가능 여부 (FR-23, FR-24)
- [ ] 교사가 직접 만든 어린이집의 중복 검증 — 같은 이름·주소가 이미 있을 때 막을지, 합칠지, 그냥 둘지 (FR-24)
- [ ] 이미 담당 교사가 있는 반을 다른 교사가 선택했을 때 — 거부 / 교체 / 계정 공유 안내 중 무엇인지 (FR-25, 테크스펙 A-6)
- [ ] 초안에 교사 이름을 **어디에 어떤 형식으로** 넣을지 — 본문 안 문구(예: "담임 OO 드림") vs 문서 메타 필드. 현재 스펙은 메타 필드 + 서버 렌더링 기준 (FR-26)
- [ ] `DraftDocument.author_teacher_id` 컬럼 추가 — 마이그레이션 시점·BE 리드 확인 필요 (FR-26)
- [ ] `Center.center_code`/`created_by_teacher_id` 컬럼 추가 — 마이그레이션 시점·BE 리드 확인 필요 (FR-23, FR-24)
- [ ] **타 반·외부 인물이 찍힌 사진 — 남은 작업** (09/15 제기) — 컬럼은 `MediaAsset.llm_allowed`로 확정됐고(09/21) 남은 것은 값을 채우고 쓰는 쪽입니다. ① **FE**: 교사 검수 화면에 표시 수단과 업로드 필드 추가 (송유진) ② **agents**: LLM 경로 게이트에서 `llm_allowed`가 false인 미디어 제외 (정은) ③ 사유 세분화(옆반 아이/외부 성인/미동의 원아)가 필요한지. 교사가 스티커로 가려 처리하는 방식은 §C의 "블러 대안 ②"와 같은 건입니다
- [ ] **[rule] H-2·`backend/domains/agents/CLAUDE.md`의 "미식별 얼굴이 남은 사진" 문구** — 교사가 미식별 얼굴을 전부 수동 분류하므로(FR-14) 서버에는 그런 사진이 도착하지 않습니다(09/15 확인). 서버가 검사할 대상이 아니라 교사 검수로 보장되는 성질이라 문구 조정이 필요합니다. 하드룰이라 팀 승인 대상
- [ ] **미분류(귀속 0건) 미디어의 서버 보존·파기 기간** — FR-19는 미승인 *초안*을 다루고 미디어 쪽은 비어 있습니다. 09/19에 확정된 파기 조건이 "귀속된 원아가 전원 만료"인 이상, **링크 0건 사진은 별도 정책이 없으면 영원히 남습니다**

- [ ] **파생본(프록시본·썸네일) 변환을 어디서 돌릴지** — 컬럼은 확정됐고(09/21, `MediaAsset.proxy_url`·`thumbnail_url`·`derivative_state`) 남은 것은 실행 위치입니다. **worker 컨테이너에 ffmpeg 의존성 추가가 필요해 BE 리드 확인 대상**이고, `media/tasks.py`에서 돌릴지는 Celery 범위 규칙(§C)과 함께 정합니다

## D. 인증

- [ ] `CurrentUser`에 담을 범위 — `class_ids`/`child_ids`를 JWT에 넣을지, 요청마다 조회할지
- [ ] Celery task의 권한 처리 — `account_id`를 인자로 넘길지, 시스템 권한으로 돌릴지

## E. 문서 관리

- [ ] **CODEOWNERS 자동 리뷰어 지정 적용** (09/19 결정, 아직 미적용) — `.github/CODEOWNERS`의 `[팀 자유 영역]`이 비어 있습니다. 공용 파일(`backend/core/`, `.env.example`, `celery_app.py`, `requirements.*`) → BE 리드, `backend/domains/<도메인>/` → 각 담당자. 공용을 위, 도메인을 아래 둡니다(마지막 일치 줄이 이김). **저장소 동작이 바뀌는 설정 변경이라 문서 PR과 분리해 별도 PR로 올립니다**
- [ ] **`/frontend/` CODEOWNERS 담당 미정** — 송유진(`@syujin2323`) 단독인지 김진하(`@wlsgk7779`)를 함께 넣는지. 2명을 넣으면 모든 프론트 PR에 2명이 리뷰어로 붙는데, CODEOWNERS 파일 자체가 경고한 패턴입니다 — "`operators`(4명)를 쓰면 3명에게 영구 미처리 알림이 남고, 그러면 진짜 신호도 같이 무시된다". 백엔드 쪽은 09/19에 반영 완료(아래 결정 기록)
- [ ] **CODEOWNERS로 `develop` 머지를 막을지** — CODEOWNERS 단독으로는 아무것도 강제하지 않습니다. `feat/*→develop` PR에서는 **리뷰어 자동 지정까지**이고, 실제 차단은 `main`에만 걸립니다(운영진 영역이라 팀이 못 바꿈). develop까지 막으려면 develop 브랜치 보호 규칙이 따로 필요한데 저장소 설정이라 팀 합의·권한 확인이 먼저입니다

- [ ] **테크스펙 경량화** — 무엇을 남기고 뺄지 합의 필요 (`doc-management` §원칙 ① 기준: FR/NFR 표·데이터 모델·파이프라인만)
- [ ] **`docs/README.md` 노션 링크 매핑** — 현재 한 줄짜리 자리표시자
- [ ] `.claude/rules/` 도입 — **보류.** 여러 폴더에 흩어진 같은 종류 파일(테스트·마이그레이션 등) 규칙이 늘어나면 그때
- [ ] `/doctor`로 주기적 점검 도입 여부 — 대화형 `claude` 터미널에서만 돌아서 데스크톱 앱 세션에서는 못 씁니다 (`doc-management` §원칙 ⑦)
- [ ] `[fix]`를 develop 직접 push로 허용하는 게 부담스러운지 — 현재 루트 §Git에 채택돼 있음

## 결정 기록

되돌리자는 이야기가 다시 나올 때 근거로 쓰는 목록입니다. 내용은 전부 반영 위치에 있으니 여기서 다시 설명하지 않습니다.

**스펙·개인정보**

- 얼굴 임베딩은 **서버 저장 + AES `bytea`**, pgvector 안 씀 (09/03) → `backend/CLAUDE.md` §DB, `domains/face`
- 사진을 초안 근거에 **포함** (09/11) → 테크스펙 NFR-09
- 사진은 **이미지 그대로** LLM에 전달, 동의 원아만 (09/12) → 루트 H-2, `domains/agents`
- 얼굴 대조 대상은 **임베딩·동의 레코드 둘 다** 확인 (09/12) → 테크스펙 0단계, `domains/face`
- 동의 철회 시 임베딩 **즉시 물리 삭제** + 이력 + `DeletionLog` (FR-22, 09/11) → `backend/CLAUDE.md` §DB
- `consent_type` 등급: 개인정보수집이용·촬영은 **필수**, 얼굴특징정보처리는 **선택** (09/13) → 테크스펙 전제조건
- 동의 **번호 ①②③ = `consent_type` 1:1** (09/20) — ①개인정보수집이용 ②활동사진영상촬영 ③얼굴특징정보처리. 거부 가능한 것은 ③뿐 → 테크스펙 전제조건·데이터 모델 ②
- 보관 기한 **졸업 후 1년** (09/12) → 테크스펙 NFR-03
- **접근 차단과 파기를 분리** (09/19) — 접근은 `Child.graduated_at`+1년으로 조회 시점 판정, 파기는 귀속 원아 전원 만료 시. `MediaAsset.retention_expires_at` **컬럼 삭제** → 테크스펙 NFR-03·NFR-03-b, `backend/CLAUDE.md` §DB
- **단체사진: 얼굴 블러 폐기.** 미동의 원아가 귀속된 사진은 S3에 올리고 **LLM 경로에서만 제외** (09/13) → 루트 H-2·H-3, `frontend/CLAUDE.md` §온디바이스, `domains/media`
- 영상도 사진과 **동일 처리** (09/14) → 테크스펙 공통 규칙
- H-2를 외부 STT까지 **넓히지 않음** (09/13) → 남은 건 위탁 계약 문제(§C에 열려 있음)
- FR-10 학부모 다운로드·공지 게시판 → **should로 내림**
- **원아별 하루 일과를 먼저 만들고 그걸 입력으로 초안 생성** (09/19, 토큰 절감) → 테크스펙 FR-27·파이프라인 5·6단계
- **영상의 미동의 원아 확인은 교사가 직접** (09/19) → 테크스펙 §공통 규칙. 보조 기능은 could
- **일정·식단 기능 제외** (09/19) → 테크스펙 §스펙 아웃
- `EvidenceBundle.source_activity_plan_id`는 **FK 없이 UUID nullable + TODO**, `context_lookup`은 `developmental_guideline`·`teacher_persona` **고정 키 JSON** (09/19 결정, 09/22 이슈 #16 코멘트 반영해 `persona`→`teacher_persona` 개명 — 원아 성향이 아니라 교사 문체) → 테크스펙 데이터 모델 ④

- `MediaAsset.redacted` → **만들지 않음**. 블러 기능이 부활하면 그때 추가 (09/21) → 테크스펙 데이터 모델 ③, `domains/media/CLAUDE.md`
- **파생본(프록시본·썸네일) 컬럼 추가** → `proxy_url`·`thumbnail_url`·`derivative_state`. "용도별 사본 금지"는 "같은 원본을 2벌 보관하지 않는다"로 한정 (09/21) → 테크스펙 데이터 모델 ③

- `MediaChildLink.method` 값 → **`face_recognition` / `manual`**, NOT NULL. `confidence_score`는 자동일 때만 (09/21) → 테크스펙 데이터 모델 ③
- **`MediaAsset.llm_allowed`** (boolean NOT NULL default false) 추가 — 타 반·외부 인물이 남은 사진을 LLM 경로에서 제외하는 게이트 (09/21) → 테크스펙 데이터 모델 ③
**온디바이스 · 파이프라인**

- **브라우저 VAD 안 씀.** 오디오를 자르지 않고 통째로 업로드 (09/14) → `frontend/CLAUDE.md` §온디바이스
- **영상·음성은 로컬 처리 안 함.** 업로드 큐만 거쳐 S3 직행. `LocalBatch`·`LocalPhoto`는 사진 전용 (09/14) → 같은 곳
- **귀속 판정은 온디바이스 얼굴 인식 단독.** 호명 발화는 초안 근거로만 (09/15) → `domains/media`
- Celery는 **LLM 호출을 포함하는 4~7단계만** → `backend/CLAUDE.md` §비동기 · Celery (STT 추가 여부는 §C에 열려 있음)
- `UnclassifiedItem` 소유 도메인 → **documents**
- **동의 수집을 앱 안으로** (09/22) — 학부모가 **원아별 초대 링크**로 가입하며 직접 동의. 교사의 서면 동의 등록(FR-01) 폐기, FR-28 신설 → 테크스펙 전제조건·FR 표·온보딩 3·B 흐름·부록 4
- `ConsentRecord` 소유 도메인 → **organization** (09/21) → `backend/README.md` §폴더 구조와 담당 범위

**컨벤션 · 문서**

- 인덴트·포매터 (TS 2 / Python 4, Prettier + Ruff) → 각 파트 `CLAUDE.md` §코드 스타일
- JSON 필드 `snake_case` → `docs/테크스펙.md` §공통 API 규약
- 브랜치 전략·이름(`<타입>/<파트>/<작업내용>`, `feat`) → `.claude/skills/create-branch`
- Squash merge, 승인 1명 → `.claude/skills/pr`
- 타임존: 컨테이너·DB는 `Asia/Seoul`, **저장은 UTC** → `backend/CLAUDE.md` §DB
- 도메인 용어 표 → 루트 §도메인 용어
- 도메인별 담당자 → `backend/README.md` §폴더 구조와 담당 범위 (09/18 확인)
- **리뷰 1명 승인 후 작성자가 직접 머지**, PR 전 `develop` 최신화 (09/19) → `.claude/skills/pr`
- **회의 결과 문서 PR은 참석자 1명이 바로 승인** (09/19) → `docs/doc-management.md` §원칙 ②
- **CI는 GitHub Actions**, 배포는 단계마다 서버 확인 후 진행 (09/19) → `.claude/skills/deploy`
- 문서 위치·갱신 규칙, 컨벤션 변경 절차(`[fix]`/`[rule]`) → 루트 §이 문서를 고칠 때
- **ADR 안 씀** — 이유 한 줄로 대체 → `docs/doc-management.md` §원칙 ④
