# 미정 사항 체크리스트

정해지면 **취소선**을 긋고 반영 위치를 적습니다. 전부 반영되면 이 파일을 지웁니다.

> 권위는 `docs/테크스펙.md`입니다. 여기서 정한 건 테크스펙에 먼저 반영하고, 그 다음 `CLAUDE.md`에 옮깁니다.

## A. 컨벤션

- [x] ~~인덴트·포매터 (TS 2 / Python 4, Prettier + Ruff)~~ → 루트 §4, `backend/CLAUDE.md` §2
- [x] ~~저장소 구조: monorepo~~ → 채택
- [x] ~~브랜치 전략 `main`/`develop` + 기능 브랜치~~ → 루트 §4
- [x] ~~머지 방식: Squash~~ → 루트 §4
- [x] ~~PR 승인 인원: 1명~~ → 루트 §4
- [x] ~~JSON 필드 케이스: snake_case~~ → `backend/CLAUDE.md` §4
- [x] ~~도메인 용어 표~~ → 루트 §2
- [ ] `.editorconfig` + pre-commit 훅 도입 여부
- [ ] **Ruff가 아직 설치·설정되지 않음** — `requirements-dev.in`에 없고 설정 파일도 없음. 루트 §6이 실행을 지시하므로 추가 필요 (의존성 추가라 BE 리드 확인)
- [ ] 에러 응답의 세부 필드명 — `detail` vs `details` vs 생략

## B. 구조 · 인프라

- [x] ~~Celery 범위를 파이프라인 4~7단계로 제한~~ → `backend/CLAUDE.md` §6
- [x] ~~컨테이너·DB 타임존 Asia/Seoul (저장은 UTC)~~ → `backend/CLAUDE.md` §5
- [ ] ONNX 모델 배포 방식: git-lfs vs S3/CDN vs `public/` 직접 포함
- [ ] 로컬 개발 환경: Docker Compose 통일 vs 각자 로컬 실행
- [ ] CI에서 막을 것: 린트 / 타입체크 / 테스트 / 빌드 중 어디까지
- [ ] `backend/.importlinter` — 채울지 지울지 (현재 0바이트. 빈 설정 파일이 제일 나쁨)

## C. 스펙 미정 (코드 구조에 영향)

- [x] ~~사진을 초안 근거에 포함할지~~ → **09/11 포함으로 결정.** NFR-09 유지, 요청사항 #2 결론 수정
- [x] ~~얼굴 임베딩 저장 위치~~ → **09/03 서버 저장(FaceEmbedding, AES `bytea`)으로 결정.** 테크스펙 L434, H-3, `domains/face/CLAUDE.md`
- [x] ~~FR-10 학부모 다운로드 / 공지 게시판~~ → **should로 내림** (테크스펙 FR 표)
- [ ] LLM에 보내는 프레임 수 상한 — 아이 1명·하루 기준 3~5장 권장, 확정값 미정 (H-2)
- [ ] FR-19 미승인 초안 보존 기간 — 테크스펙에 `?시간`으로 비어 있음
- [ ] 요구사항 ID 공란(FR-02) 재정렬 시점
- [x] ~~동의 철회 흐름~~ → **FR-22 신설(09/11).** 철회 시 `FaceEmbedding` 즉시 물리 삭제 + `consent_revoked` 이력 + `DeletionLog`
- [x] ~~미동의 아동 제외 방식 — 제외 vs 블러 후 제외~~ → ~~09/12 블러 처리~~ **→ 09/13 블러 폐기. 아래 "단체사진 처리" 항목이 최종입니다** (업로드는 하고 LLM 경로에서만 제외)
- [x] ~~위 결정과 `LocalPhoto.excluded_reason=미동의포함`·루트 H-3 문구의 정합성~~ → **정리 완료.** `미동의포함`은 제외 사유에서 빠집니다 (남는 값: `부적절`/`기타`). 업로드는 하고 서버가 LLM 경로에서 제외 (테크스펙 `LocalPhoto`, `frontend/CLAUDE.md` §6, 루트 H-3)
- [x] ~~NFR-03 보관 기한 — "졸업 후 1년" vs F-3 "3개월"~~ → **09/12 졸업 후 1년으로 확정.** `retention_expires_at` = `graduated_at` + 1년 (테크스펙 NFR-03·F-3·MediaAsset, `backend/CLAUDE.md` §5)
- [ ] 보관 기간 안에서 원본 → 저화질(`storage_tier`) 전환 시점 — 같은 객체를 초안 맥락 추출에도 쓰므로, 저화질 전환 후 초안을 재생성(FR-18)하면 열화된 사진으로 맥락을 뽑게 됨
- [x] ~~미식별 얼굴 블러본(`blob_redacted`)이 올라간 경우 학부모도 블러본을 받는지~~ → **해당 없음.** 09/13 블러 폐기로 블러본 자체가 생기지 않습니다
- [x] ~~단체사진 처리 (전원 동의 전제 여부)~~ → **09/13 리뷰 회의 최종 결정.** 블러 폐기(스파이크 결과 인식은 되나 블러 영역이 과도하게 큼). **미동의 원아가 귀속된 사진은 LLM에 보내지 않고**, S3 업로드는 필수 동의(1번) 범위라 그대로 합니다 (테크스펙 "공통 규칙"·요청사항 #2, 루트 H-2·H-3, `frontend/CLAUDE.md` §6, `domains/agents`·`domains/media`)
- [ ] 블러 대안 — 추후 ① 현행 유지(LLM 근거에서 제외) ② 교사에게 직접 사진 편집 지시(스티커 삽입 등) ③ 블러 재시도(구현 난이도 높음) 중 검토. **지금은 ①로 갑니다**
- [ ] `MediaAsset.redacted` 컬럼 — 블러 폐기로 항상 false. 유지할지 지울지 (마이그레이션 전에 정하면 됨)
- [x] ~~영상에 미동의 원아가 담긴 경우 — 전체 제외 / 구간 편집 / 운영 지침~~ → **09/14 결정. 사진과 동일하게 처리합니다.** S3 업로드는 필수 동의(1번) 범위라 그대로 올리고 LLM 경로에서만 제외합니다. 전체 제외·구간 편집은 채택하지 않음 (테크스펙 "영상·음성 메모 — 로컬 처리하지 않음", "공통 규칙")
- [ ] 미동의 원아가 함께 찍힌 사진의 **학부모 열람·다운로드**(FR-09/10) 노출 여부 — LLM 경로는 막았지만 앨범 노출은 별개 결정
- [ ] **외부 STT 제공자 선정** (09/13 STT 서버 전환). 소음 환경 정확도, 한국어, 위탁 처리 계약·보관 정책(학습 이용 여부), 비용
- [x] ~~[rule] H-2의 적용 범위를 외부 STT까지 넓힐지~~ → **넓히지 않습니다(09/13).** H-2는 "누구 이야기인지"를 가리는 규칙이고 발화 내용 자체는 근거로 전달합니다. LLM에는 3단계 비식별화를 거친 텍스트만 가므로 STT 전환으로 H-2가 깨지지 않습니다. 남는 건 규칙이 아니라 위탁 계약·동의서 문제(아래 항목)
- [ ] **[rule] `backend/CLAUDE.md` Celery 범위** — 현재 "LLM 호출을 포함하는 파이프라인 4~7단계만". STT가 서버로 오면서 1-B가 새 장시간 작업이 되는데, 동기로 두면 요청 타임아웃이 납니다. 문구를 1-B까지 넓힐지 BE 리드 확인 필요
- [x] ~~VAD(발화 구간 탐지) 위치~~ → **09/14 결정. 브라우저 VAD를 쓰지 않습니다.** 오디오를 자르지 않고 통째로 올리고 발화 구간 판정은 외부 STT에 맡깁니다. `@ricky0123/vad-web`은 기술스택에서 제외 (테크스펙 파이프라인 1-B·온디바이스 모델 표·기술스택, `frontend/CLAUDE.md` §6)
- [x] ~~영상·음성 메모의 로컬 처리 방식~~ → **09/14 결정. 로컬 처리하지 않습니다.** 영상·음성은 브라우저에서 분류·검수·편집하지 않고 업로드 큐만 거쳐 S3로 직행합니다. `LocalBatch`·`LocalPhoto`는 사진 전용 (테크스펙 "영상·음성 메모 — 로컬 처리하지 않음", `frontend/CLAUDE.md` §6)
- [x] ~~귀속 판정(2단계)이 서버로 옮겨간 데 따른 후속~~ → **09/15 결정. 귀속 판정은 온디바이스 얼굴 인식 단독입니다.** 호명 발화는 귀속에 쓰지 않고 초안 작성 근거로만 사용하므로 서버에서 원아 명단을 다룰 일도, 두 판정을 병합할 일도 없습니다 (테크스펙 FR-04·파이프라인 2단계·기술 선택 근거, `backend/domains/media/CLAUDE.md`)
- [ ] `MediaChildLink.method`의 자동 분류 값 이름 — 기존 `face_name_cross_check`는 09/15 호명 교차검증 폐기로 의미가 맞지 않음. 마이그레이션 전에 확정
- [ ] 동의서·위탁계약서에 **음성 처리 외부 위탁** 문구 추가 (09/13 STT 전환)
- [ ] 영상·음성 파일이 없는 날의 서비스 동작
- [x] ~~사진을 LLM에 어떤 형태로 넘길지~~ → **09/12 이미지 그대로 전달로 결정.** 동의받은 원아 사진만. 테크스펙 요청사항 #2·파이프라인 5단계, 루트 H-2, `domains/agents/CLAUDE.md`
- [ ] 영상 중간 프레임을 사진처럼 초안 근거로 쓸지 — 쓴다면 몇 장·선택 기준 (파이프라인 1단계 `frame_extract`)
- [ ] 동의서 법적 문구 확정 (테크스펙 데이터모델 ② "수정 필요")
- [x] ~~얼굴 대조 대상 판정 기준 — 임베딩 존재만 vs 동의 레코드도 함께~~ → **09/12 둘 다 확인으로 결정.** 어긋나면 제외되는 쪽으로 실패시키기 위함 (테크스펙 0단계, `domains/face/CLAUDE.md`)
- [x] ~~그때 보는 `consent_type`~~ → **09/13 결정.** 개인정보수집이용·활동사진영상촬영은 **필수**, 얼굴특징정보처리는 **선택**(미동의여도 알림장 서비스 이용 가능, 임베딩만 미등록). 얼굴 대조는 얼굴특징정보처리 동의를 봅니다 (테크스펙 전제조건·`ConsentRecord`)
- [ ] 동의 항목의 정식 명칭·화면 문구 — 회의에서 "1번/2번"으로 부른 것을 `consent_type` 값과 1:1로 맞춰 적을 것
- [ ] `ConsentRecord` 소유 도메인 — face vs organization
- [ ] 어린이집 가입 코드(`center_code`) 형식·발급 방식 — 길이, 문자셋, 재발급 가능 여부 (FR-23, FR-24)
- [ ] 교사가 직접 만든 어린이집의 중복 검증 — 같은 이름·주소가 이미 있을 때 막을지, 합칠지, 그냥 둘지 (FR-24)
- [ ] 이미 담당 교사가 있는 반을 다른 교사가 선택했을 때 — 거부 / 교체 / 계정 공유 안내 중 무엇인지 (FR-25, 테크스펙 A-6)
- [ ] 초안에 교사 이름을 **어디에 어떤 형식으로** 넣을지 — 본문 안 문구(예: "담임 OO 드림") vs 문서 메타 필드. 현재 스펙은 메타 필드 + 서버 렌더링 기준 (FR-26)
- [ ] `DraftDocument.author_teacher_id` 컬럼 추가 — 마이그레이션 시점·BE 리드 확인 필요 (FR-26)
- [ ] `Center.center_code`/`created_by_teacher_id` 컬럼 추가 — 마이그레이션 시점·BE 리드 확인 필요 (FR-23, FR-24)
- [x] ~~`UnclassifiedItem` 소유 도메인~~ → **documents.** `ref_type`으로 여러 종류를 참조하고 서버 사유만 기록
- [ ] **문서 게시(노출) 모델 — 승인/게시 분리 여부** (09/16 documents 담당 제기) — 테크스펙 ERD는 `DraftDocument.status`(draft/verified/approved)와 `RevisionLog.action`(revoke/resend)만으로 노출·회수·재게시를 표현하고, `approved_at`을 노출 기준 시점으로 봅니다. documents 도메인 골격 작업 중 별도 `DocumentPublication`(게시 회차) 테이블로 승인과 게시를 분리하고, 재게시마다 새 회차·새 24시간 회수 기한을 갖는 안을 제안했습니다(24시간·미열람 조건 자체는 테크스펙 파이프라인 9단계와 동일, 표현 방식만 다름). 채택 시 테크스펙 ERD·`RevisionLog.action` 값 갱신이 필요합니다 — `backend/domains/documents/models.py`(`feat/be/documents-audit-skeleton` 브랜치) 참고, BE 리드 확인 필요
- [ ] **타 반·외부 인물이 찍힌 사진** (09/15 제기) — 옆반 아이·외부 성인은 우리 반 명단에 없어 자동 식별도 교사 수동 귀속도 불가능합니다. `MediaChildLink`가 전부 채워져도 사진에는 남아 있어 "귀속 완료된 깨끗한 사진"으로 보이고 LLM 경로로 나갑니다. 우리에게 동의를 준 적 없는 인물이라 H-2가 막으려던 상황입니다. 후보: ① 교사 검수에 표시 수단 추가 → LLM 제외 ② 기존 `excluded_reason=기타`로 교사가 업로드에서 제외 ③ 현행 유지. **①로 갈 경우 데이터 모델 변경 수반** — `MediaAsset`에 교사 표시 컬럼(예: `has_unconsented_person`, 불리언) 제안. 사유 세분화(옆반 아이/외부 성인/미동의 원아)가 필요한지도 함께 결정 (FE 화면 추가 + BE 리드 확인)
- [ ] **[rule] H-2·`backend/domains/agents/CLAUDE.md`의 "미식별 얼굴이 남은 사진" 문구** — 교사가 미식별 얼굴을 전부 수동 분류하므로(FR-14) 서버에는 그런 사진이 도착하지 않습니다(09/15 확인). 서버가 검사할 대상이 아니라 교사 검수로 보장되는 성질이라 문구 조정이 필요합니다. 하드룰이라 팀 승인 대상
- [ ] **미분류(귀속 0건) 미디어의 서버 보존·파기 기간** — FR-19는 미승인 *초안*을 다루고 미디어 쪽은 비어 있습니다. 아래 보관기한 항목의 파기 조건이 "링크가 1건 이상 있고 그 아이들이 전원 만료"인 이상, 링크 0건 사진은 별도 정책이 없으면 영원히 남습니다
- [ ] **NFR-03 재구성 제안** (09/15 논의) — 접근 차단과 파기를 분리합니다. ① 학부모 접근 차단은 **그 아이의** `graduated_at` + 1년 기준으로 조회 시점에 판정 ② 파기는 **링크된 아이 전원**이 만료된 사진만 배치로 삭제. 한 사진에 졸업·퇴소일이 다른 아이가 여럿이면 `MediaAsset.retention_expires_at`이 한 값으로 정해지지 않기 때문입니다. 이 방향이면 **`retention_expires_at` 컬럼을 삭제**하고 파기 배치가 조인해서 계산합니다 (파생값이고, 갱신 트리거가 organization의 퇴소 처리에 걸려 도메인 간 쓰기 결합이 생김). NFR-03 문구를 두 문장으로 나눠야 함
- [ ] **영상 프록시본·썸네일** — 화면이 요구하는 "H.264 프록시본"(귀속 충돌·초안 검토의 근거 재생, FR-07)이 데이터 모델 ③의 "용도별 사본을 만들지 않는다"와 충돌합니다. 아이폰 HEVC/.MOV와 HEIC는 사파리 외 브라우저에서 재생·표시가 안 됩니다. 제안: 그 문구를 "같은 원본을 용도마다 따로 업로드해 2벌 보관하지 않는다"로 한정하고, **원본에서 재생성 가능한 파생본은 허용**. 수반되는 것 — `MediaAsset`에 `proxy_url`·`thumbnail_url`(nullable)·`derivative_state` 컬럼, worker 컨테이너에 ffmpeg 의존성 추가(BE 리드 확인), `media/tasks.py`에서 변환 수행(Celery 범위 규칙과 함께 결정)

## D. 인증

- [ ] `CurrentUser`에 담을 범위 — `class_ids`/`child_ids`를 JWT에 넣을지, 요청마다 조회할지
- [ ] Celery task의 권한 처리 — `account_id`를 인자로 넘길지, 시스템 권한으로 돌릴지

## E. 문서 관리

- [x] ~~문서 위치와 갱신 규칙~~ → 루트 §7 (저장소 먼저, Notion 나중)
- [x] ~~ADR 사용 여부~~ → **쓰지 않음.** 이유 한 줄로 대체 (사람만 볼 이유는 HTML 주석)
- [x] ~~컨벤션 변경 절차~~ → 루트 §7 (`[fix]` 직접 push / `[rule]` PR + 승인)
- [ ] 도메인별 담당자 확정 — 7개 도메인 중 `agents`만 정해짐. **09/13 AI 역할 분담을 논의했으나 회의 중 네트워크 문제로 미확정**
- [x] ~~[rule] 브랜치 이름에 파트 구분 넣기~~ → **09/14 결정.** `<타입>/<파트>/<작업내용>`, 타입은 `feature`가 아니라 커밋 타입과 같은 `feat`, 파트는 `ai`/`be`/`fe`. 예: `feat/be/agent-pipeline` (루트 §4)
