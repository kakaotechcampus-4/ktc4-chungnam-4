# 문서 관리 방식

> 2026-09-11 제안 · 09-12 현황 반영
> **§1 문제 진단은 제안 시점(분할 전) 기준입니다.** §2 제안은 대부분 이미 반영돼 있고, 남은 것은 §4에 표로 정리했습니다.
> 문서를 새로 만들거나 정리할 때 이 파일을 기준으로 삼습니다.

## 1. 무엇이 문제였나 (09-11 시점)

- **노션이 source of truth인데 Claude가 못 읽습니다.** 카테캠 노션이라 에이전트 권한이 막혀 있습니다.
- **노션 전용 블록은 복사도 안 됩니다.** 저장소로 옮기려면 손으로 다시 치거나 캡처해야 합니다.
- **회의 후 어디에 적을지부터 헷갈립니다.** 테크스펙 + 파트별 문서(AI/FE/BE) + CLAUDE.md + 저장소 테크스펙 4종이라 반영 위치가 불명확했습니다.

당시 CLAUDE.md 자체의 문제도 있었습니다.

- 스펙 변경은 "테크스펙 먼저"만 있고 **어느 테크스펙인지**(노션? 저장소?)가 없었습니다. → 루트 §3에서 `docs/테크스펙.md`로 명시 (해결)
- **CLAUDE.md 자신을 어떻게 고치는지**가 없었습니다. → 루트 §7 신설 (해결)
- 문서 관리 항목 3개가 미결 체크박스였습니다. → `open-questions.md` E에서 전부 확정 (해결)
- **568줄 전부가 매 세션 로드**됐습니다. 프론트 작업에 DB 컨벤션까지 딸려왔습니다. → 계층 분할 (해결)

## 2. 원칙

### ① 테크스펙 — 저장소가 FR/NFR의 원본

저장소 `docs/테크스펙.md`를 **코드 작성에 필요한 것만** 남겨 경량화합니다 (FR/NFR 표, 데이터 모델, 파이프라인). 배경·시장·일정은 노션에만 둡니다 — 코드 작업의 기준으로 참조하지 않습니다.

**순서: 회의 후 저장소를 먼저 고치고 → 노션에 반영합니다.** 노션을 먼저 고치지 않습니다. 순서가 깨지면 어느 게 최신인지 알 수 없습니다.

<!-- 경량화 사본이라고 부르지 않는 이유: 루트 CLAUDE.md가 "권위 순서: 테크스펙(FR/NFR) > 이 문서"로 선언했고 ①이 저장소를 먼저 고치라고 하므로, FR/NFR에 관한 한 저장소가 원본이고 노션이 사본입니다. 사본이 원본을 이길 수 없습니다. -->

### ② CLAUDE.md — 코드와 직접 연관되므로 가장 중요

회의가 끝나면 즉시 고칩니다. 계층으로 나눠 필요한 사람에게만 붙게 합니다.

변경 등급 2개 (루트 §7-2):
- `[fix]` 문서가 현실과 다른 것 → develop 직접 push + 채널 1줄 공지
- `[rule]` 규칙 자체가 바뀜 → PR + 팀 승인

### ③ 회의 결과는 체크리스트로

`docs/open-questions.md` 하나에 모읍니다. 파트별로 노션에 따로 두지 않습니다. 정해지면 취소선, 전부 반영되면 파일을 지웁니다. 파트별 확인이 필요하면 저장소 문서(CLAUDE.md + 테크스펙)를 기준으로 Claude Code에 물어봅니다.

### ④ 결정 이유를 한 줄로

CLAUDE.md가 "무엇"만 적고 "왜"를 안 적으면 두 달 뒤 "이거 왜 이렇게 했지"가 나옵니다. 근거 요구사항 ID를 같이 답니다.

```
- 얼굴 임베딩은 AES 암호화 후 bytea로 저장하고, pgvector를 쓰지 않습니다.
  암호화하면 벡터 간 거리 관계가 깨져 SQL로 유사도를 계산할 수 없기 때문입니다 (NFR-01).
```

**판단에 필요한 "왜"는 본문에, 사람만 볼 경위는 HTML 주석에 씁니다** (루트 §7-3). 주석은 컨텍스트에 주입되기 전에 제거되므로, 주석에 넣은 이유는 Claude가 보지 못합니다. 위 예시의 "거리 관계가 깨진다"는 Claude가 대안을 제안하지 않으려면 알아야 하므로 본문이고, "09-03 회의에서 되돌림" 같은 경위는 주석이 맞습니다.

### ⑤ 고친 뒤 전파

채널에 1줄 공지 → 그 영역 작업 중이면 "CLAUDE.md 다시 읽어줘" → 하드룰이 바뀌었으면 세션 재시작. **기존 코드는 소급하지 않습니다** (하드룰 위반만 예외).

### ⑥ 우선순위

작업 중 참조 1순위는 CLAUDE.md, 요구사항이 충돌할 때 최종 근거는 테크스펙(FR/NFR)입니다. 하드룰 4개가 전부 NFR에서 파생됐으므로 테크스펙이 근거의 출처입니다.

### ⑦ 한 번 구현하면 그 자체가 명세가 되는 것

API 구조·데이터 모델처럼 코드가 곧 명세가 되는 것은 **매 세션 컨텍스트에 올릴 필요가 없습니다.** 테크스펙이나 도메인 문서에 두고, CLAUDE.md에는 규칙만 남깁니다.

## 3. 절차 — 문서를 고쳐야 할 때

### 언제 발동하나

| 상황 | 첫 행동 |
|---|---|
| **회의가 끝났다** | 결정 목록을 만들어 아래 배치표대로 나눠 적습니다. **같은 날 합니다** — 미루면 무엇을 정했는지 기억이 갈립니다 |
| **작업 중 문서가 코드·현실과 다른 걸 발견했다** | `[fix]`로 바로 고칩니다 |
| **규칙을 바꾸면 좋겠다는 생각이 들었다** | 문서를 직접 고치지 말고 `open-questions.md`에 **제안으로** 올리고, **디스코드 채널에도 한 줄 올립니다.** 혼자 든 생각은 아직 팀 규칙이 아닙니다 — 다른 사람이 반대할 기회가 있어야 `[rule]` 승인이 형식적이지 않습니다 |
| **멘토 리뷰를 받았다** | 코드 반영은 `refactor/…` 브랜치, 규칙으로 남길 것은 `[rule]` |
| **코드를 짜다 스펙에 근거가 없는 값을 만났다** | 상상해서 채우지 말고 `open-questions.md`에 질문으로 올립니다 (루트 §6) |
| **PR 리뷰에서 문서와 다른 코드가 나왔다** | 문서가 틀린 건지 코드가 틀린 건지 **먼저 정합니다.** 문서가 틀렸으면 `[fix]`, 코드가 틀렸으면 코드를 고칩니다 |
| **한동안 안 봤다** | 문서 간 충돌 점검 + `/doctor` (§5 주의 참고) |

### 어디에 적나

| 바뀐 것 | 파일 |
|---|---|
| 요구사항·데이터 모델·파이프라인 | `docs/테크스펙.md` **먼저** |
| 전원이 지킬 규칙, 용어, Git | 루트 `CLAUDE.md` |
| 백엔드/프론트 전반 | `backend/CLAUDE.md` · `frontend/CLAUDE.md` |
| 한 도메인 안의 것 | `backend/CLAUDE.md` (도메인별 문서는 두지 않음 — §5) |
| 아직 안 정해진 것 | `docs/open-questions.md` |

**가장 좁은 곳에 적습니다.** face에만 해당하는 규칙을 루트에 올리면 전원이 매 세션 그걸 읽습니다.

### 순서

1. **스펙이 바뀌었으면 테크스펙 먼저**, 그 다음 CLAUDE.md. 코드가 스펙을 앞서지 않습니다 (루트 §3).
2. **등급을 정합니다** — `[fix]`는 develop 직접 push + 채널 1줄, `[rule]`은 PR + 팀 승인.
3. **이유 한 줄과 요구사항 ID를 답니다** (②④). 판단에 필요한 이유는 본문, 경위는 HTML 주석.
4. **저장소를 고친 뒤 노션에 반영합니다.** 반대로 하지 않습니다 (①).
5. **전파합니다** — 채널 1줄 → 그 영역 작업자는 "CLAUDE.md 다시 읽어줘" → 하드룰이면 세션 재시작 (⑤).

### 하나를 고치면 같이 봐야 하는 것

문서가 계층으로 나뉘어 있어서, 한 곳만 고치면 다른 곳이 조용히 어긋납니다.

| 고친 것 | 같이 볼 것 |
|---|---|
| **용어표** | 그 단어를 쓰는 도메인 문서, 테크스펙의 enum 값 |
| **하드룰(H-n)** | `backend/CLAUDE.md` 필수 테스트 목록, "H-n이 직접 걸리는 도메인" 문서 |
| **데이터 모델** | 소유 도메인 문서의 테이블 표. 마이그레이션 PR이면 테크스펙 ERD도 (루트 §4) |
| **요구사항 ID 신설·등급 변경** | 그 ID를 참조하는 흐름표·도메인 문서의 요구사항 줄 |
| **`open-questions.md`에서 해결 처리** | 테크스펙에 먼저 반영됐는지 (③) |

## 4. 파일 구조

```
CLAUDE.md                     ← 항상 로드 (142줄): 하드룰, 용어표, Git, 문서 지도
backend/CLAUDE.md             ← 백엔드 만질 때 (129줄)
frontend/CLAUDE.md            ← 프론트 만질 때 (85줄)
docs/
  테크스펙.md                  ← FR/NFR 원본 (1,289줄 — 경량화 예정)
  open-questions.md           ← 미정 체크리스트
  doc-management.md           ← 이 파일
  README.md                   ← 노션 링크 매핑 (미작성)
```

하위 CLAUDE.md는 **그 폴더의 파일을 읽을 때만** 로드됩니다. 내 도메인 문서는 나한테만 붙고 프론트 작업자한테는 안 붙습니다.

> 백엔드 작업 시 약 288줄, 프론트 약 229줄. 분할 전에는 전원이 568줄을 받았습니다.

**크기 목표는 파일당 200줄 이하입니다** (Claude Code 공식 권장). 넘으면 어댑턴스가 떨어집니다. 현재 전부 목표 안에 있고, 테크스펙만 예외입니다 — 테크스펙은 항상 로드되지 않고 필요할 때만 읽히므로 허용합니다.

## 5. 남은 일

| 항목 | 상태 |
|---|---|
| CLAUDE.md 계층 분할 | 완료 (`db5e41a`) |
| 도메인별 CLAUDE.md | **09-12 폐지.** 내용 대부분이 테크스펙 사본이었고 담을 규칙이 아직 없어 YAGNI 위반. 고유 규칙 5개만 `backend/CLAUDE.md`로 흡수 |
| **빈 `.py` 파일 정리** | **미착수** — `ef66d8e`가 깔아둔 20여 개가 0바이트. 같은 YAGNI 지적 대상 |
| 변경 등급 `[fix]`/`[rule]` | 루트 §7-2에 반영 |
| 회의 체크리스트 일원화 | `open-questions.md` 운영 중 |
| **테크스펙 경량화** | **미착수** — 1,289줄. 무엇을 남기고 뺄지 합의 필요 |
| **`docs/README.md` 노션 링크 매핑** | **미작성** — 현재 한 줄짜리 자리표시자 |
| `.claude/rules/` 도입 | 보류 — 여러 폴더에 흩어진 같은 종류 파일(테스트·마이그레이션 등) 규칙이 늘어나면 그때 |
| `/doctor`로 주기적 점검 | 미도입 |

### `/doctor` 사용 시 주의

`/doctor` 점검은 체크인된 CLAUDE.md에서 **코드로 알 수 있는 내용**(디렉터리 구조, 의존성 목록, 아키텍처 개요)을 덜어내고 **함정·근거·기본값과 다른 관례**는 남기라고 제안합니다. Claude Code v2.1.206 이상이 필요하고, **대화형 `claude` 터미널에서만 실행됩니다** — 데스크톱 앱 세션에서는 쓸 수 없습니다.

## 6. 아직 의견을 받는 것

- `[fix]`를 develop 직접 push로 허용하는 게 부담스러운지 (현재 루트 §4에 채택돼 있음)
- 테크스펙 경량화에서 무엇을 남기고 무엇을 뺄지
