# 아이담 — 팀 공통 규칙

> 영상·사진·음성 속 맥락을 수집해 원아의 하루를 담아내는 **AI 알림장·관찰일지 자동 작성 서비스**

전원이 항상 지켜야 하는 것만 둡니다. 파트·도메인 규칙은 아래 지도를 따라가세요.

<!-- 200줄 이하 권장(Anthropic memory 가이드). 루트는 항상 로드되므로 가장 짧게 유지한다. -->

## 문서 지도

| 문서 | 언제 읽히나 | 내용 |
|---|---|---|
| `CLAUDE.md` (이 파일) | 항상 | 절대 규칙, 용어, Git, Claude 규칙 |
| `backend/CLAUDE.md` | backend 파일을 열 때 | 구조, API·DB 규약, 테스트 |
| `frontend/CLAUDE.md` | frontend 파일을 열 때 | 구조, 상태관리, 온디바이스 |
| `docs/테크스펙.md` | 요구사항 확인이 필요할 때 | **FR/NFR 원본. 충돌 시 이쪽이 기준** |
| `docs/open-questions.md` | 합의 전 항목 확인 | 미정 사항 체크리스트 |
| `docs/doc-management.md` | **문서를 새로 만들거나 정리할 때** | 문서 관리 방식, 파일 구조, 남은 일 |

**권위 순서: 테크스펙(FR/NFR) > 이 문서 > 하위 CLAUDE.md.** 이 문서의 하드룰은 전부 NFR에서 나옵니다. 둘이 어긋나면 테크스펙이 맞고, 이 문서를 고칩니다.

---

## 1. 절대 규칙 (Hard Rules)

> 이 4개는 **위반 시 법적 리스크 또는 서비스 신뢰 붕괴**로 이어집니다. PR에서 발견되면 다른 리뷰 의견과 무관하게 머지 불가.

### H-1. 승인 플래그 없는 저장·전송 금지 (NFR-06, FR-08)

교사 승인(`status == approved`)을 통과하지 않은 문서는 **저장·복사·노출·전송 함수에 절대 넣지 않습니다.**

```python
# ✅ 노출 경로는 단일 함수를 통과하게 만든다
def publish_to_parent(draft: DraftDocument) -> None:
    assert_approved(draft)      # 여기서만 게이트를 통과
    ...

# ❌ 승인 검사 없이 직접 노출
return {"content": draft.content}
```

노출·전송 로직은 **한 군데**에만 두고, 다른 곳에서 응답 body에 초안 본문을 직접 담지 않습니다.

### H-2. LLM에는 비식별 데이터만

- 원아 실명·생년월일·학부모 정보는 LLM 요청에 들어가지 않습니다. 이름은 파이프라인 3단계에서 `CHILD_A` 형태 토큰으로 치환된 뒤 전달합니다.
- 실명 ↔ 토큰 매핑은 서버 내부에만 존재하며, 프롬프트·로그·트레이싱(Langfuse) 어디에도 실명이 남지 않게 합니다.
- **사진은 동의받은 원아의 것만 이미지 그대로 넘어갑니다** (NFR-09). 미동의 원아 얼굴은 업로드 전 로컬 블러로 걸러지므로 서버에서 LLM으로 나가는 사진에는 남아 있지 않습니다 (H-3).

### H-3. 얼굴 임베딩은 온디바이스에서, 미동의 원아 얼굴은 LLM에 금지 (NFR-01)

- 얼굴 검출·임베딩 생성은 **브라우저 온디바이스**에서 수행합니다. **얼굴 등록용 사진은 서버로 보내지 않고** 임베딩만 올립니다.
- 임베딩은 서버 `FaceEmbedding`에 **애플리케이션 레벨 AES 암호화 후 `bytea`로** 저장합니다. 키는 DB 외부(KMS/시크릿)에 둡니다.
- **동의하지 않은 원아의 얼굴이 담긴 원본 사진은 LLM에 보내지 않습니다** (NFR-01). 미동의 원아 얼굴은 업로드 전 로컬에서 블러 처리되므로, 서버에 도달한 사진에는 미동의 얼굴이 남아 있지 않습니다.
- **일상 촬영분의 서버 업로드 자체는 막지 않습니다.** S3에 올린 **같은 원본 하나**를 학부모 열람·다운로드(FR-09/10)와 초안 맥락 추출(NFR-09)이 공유합니다. 단 **교사가 브라우저에서 확정한 것만** 올라갑니다. 분류 실패분은 서버로 올리지 않고 교사가 로컬에서 직접 분류합니다.
  분류 실패 사진에는 **동의하지 않은 원아가 찍혀 있을 수 있고, 서버에 올라간 뒤에는 되돌릴 수 없기 때문입니다.**
- 브라우저 `IndexedDB` 캐시는 **배치 처리 동안만** 두고 배치 종료 시 폐기합니다.

<!-- 09/03 회의: 기기 분실·고장 시 얼굴정보 전량 유실 문제로 온디바이스 저장 → 서버 저장으로 되돌림. 테크스펙 L434 -->

### H-4. 로그는 남기고, 개인정보는 남기지 않는다

- 열람은 전부 `AccessLog`에, 파기는 전부 `DeletionLog`에 기록합니다 (NFR-04, NFR-05).
- 반대로 애플리케이션 로그(stdout)에는 실명·전화번호·이메일·토큰·임베딩 값을 찍지 않습니다. 식별이 필요하면 ID만 남깁니다.

---

## 2. 도메인 용어

**한 가지 단어만 씁니다.** 새 용어가 필요하면 이 표에 추가한 뒤 사용합니다.

| 의미 | 코드에서 쓰는 말 | 쓰지 않는 말 |
|---|---|---|
| 원아 | `child` | kid, student, baby |
| 반 | `class_` / `klass` (Python 예약어 회피) | group, room |
| 관찰일지 | `observation_log` | journal, diary |
| 알림장 | `parent_note` | notice(공지와 혼동), letter |
| 공지 | `notice` | announcement, board |
| 근거 | `evidence` | source, proof |
| 미분류함 | `unclassified` | pending, unknown |
| 승인 | `approve` / `approved` | confirm, publish(노출과 구분) |

---

## 3. 소유권

리드 — **FE 송유진 / BE 엄태은 / AI·에이전트 한상균**

- 리드 영역의 **구조 변경**(디렉터리 추가, 의존성 추가, 공통 모듈 수정)은 해당 리드 리뷰를 받습니다.
- **스펙(요구사항 ID, 데이터 모델, 인터페이스) 변경은 `docs/테크스펙.md`를 먼저 고치고** PR 본문에 링크합니다. 코드가 스펙을 앞서가면 안 됩니다.
- 작업 시작 전 이슈/스레드에 "나 이거 잡는다"를 남깁니다.

---

## 4. Git

```
main        # 보호 브랜치. 매주 수요일 develop → main PR 1회 → 멘토 리뷰 → approve 시 merge
develop     # 기본 작업 브랜치
feat/…      # 기능 단위. develop에서 분기 → develop으로 PR
refactor/…  # 멘토 피드백 반영. develop에서 분기 → develop으로 PR
```

- **develop에 직접 push하지 않습니다.** 문서 오타 수정 등 `[fix]` 급만 예외.
- 브랜치명은 영어 kebab-case이고 **작업 내용까지 붙입니다** — `refactor/week1-mentor-feedback`. 같은 영역을 여러 주에 걸쳐 건드리므로 이름이 겹치면 로그를 못 읽습니다.
- 커밋: Conventional Commits (`feat` `fix` `refactor` `chore` `docs` `test` `style`). 제목은 영어 명령형 50자 이내, 본문은 한국어로 **"왜"** 를 씁니다.
- **포맷팅 커밋과 로직 커밋을 섞지 않습니다. 문서 커밋과 코드 커밋도 섞지 않습니다.**
- PR **300줄 이하** 목표. 리뷰어 1명 승인 + CI 통과 후 **Squash merge**.
- PR 본문에 **관련 요구사항 ID**(`FR-04`, `NFR-06`)를 답니다. ID를 못 적으면 근거 없는 코드라는 신호입니다.
- **마이그레이션이 포함된 PR은 테크스펙의 ERD도 같이 갱신**합니다.
- 리뷰 코멘트는 `[must]` / `[ask]` / `[nit]`로 구분해서 씁니다.

- 코드·식별자·커밋 제목은 **영어**, PR 본문·주석·문서는 **한국어** (테스트 함수명은 한국어 허용 — `backend/CLAUDE.md` §7).
- 개행은 LF입니다. `.gitattributes`가 강제하므로 따로 설정할 게 없습니다.
- 커밋 금지(`.gitignore` 참고): `.env*`, `*.pem`, `*.key`, `*.onnx`, `uploads/`, `media/`
- **원아 사진·영상·음성을 테스트 픽스처로 커밋하지 않습니다.** 합성 데이터나 팀원 본인 사진을 씁니다.

---

## 5. 환경변수

- 코드에 URL·키·비밀번호를 박지 않습니다. `.env.example`에는 **키 이름과 설명만** 커밋합니다.
- 키가 커밋됐다면 되돌리는 것으로 끝내지 말고 **즉시 폐기·재발급**합니다.

---

## 6. Claude 작업 규칙

- 코드를 쓰기 전에 §1과 해당 파트 `CLAUDE.md`를 확인하고, 변경 이유를 요구사항 ID(`FR-05` 등)로 답니다.
- **스펙에 근거가 없는 값은 상상해서 채우지 않고 "미정"으로 남기고 질문합니다.**
- **대화 중 규칙을 바꾸자는 이야기가 나오면 문서를 바로 고치지 않습니다.** `docs/open-questions.md`에 제안으로 올리고, **디스코드 채널에도 공유하라고 질문자에게 말합니다.** `[rule]`은 팀 승인 대상입니다 (`docs/doc-management.md`).
- 먼저 확인받을 것: 의존성 추가·삭제, 디렉터리 구조 변경, DB 스키마 변경, 마이그레이션 생성, 새 패턴 도입
- 하지 않기: 승인·권한 검사 우회(H-1) / 동의 확인 우회(H-3) / 실명·연락처 삽입(H-2) / 테스트 약화·스킵 / 요청 범위를 넘는 리팩터링(**보고만** 합니다)
- 코드 변경을 마치면 린터·포매터를 돌리고 결과를 보고합니다 — BE `ruff check --fix && ruff format`, FE `npm run lint -- --fix`. 설치돼 있지 않으면 재시도하지 말고 그 사실을 보고합니다.

---

## 7. 이 문서를 고칠 때

`docs/doc-management.md`를 따릅니다 — 반영 순서, `[fix]`/`[rule]` 등급, 이유 남기는 법, 전파 방법이 전부 거기 있습니다.

> **계속 지켜야 할 규칙은 채팅으로 말하지 말고 문서에 적으세요.** 대화로만 준 지시는 컨텍스트가 압축되면 사라집니다.
