# 아이담 — 팀 공통 규칙

> 영상·사진·음성 속 맥락을 수집해 원아의 하루를 담아내는 **AI 알림장·관찰일지 자동 작성 서비스**

전원이 항상 지켜야 하는 것만 둡니다. 파트·도메인 규칙은 아래 지도를 따라가세요.

<!-- 200줄 이하 권장(Anthropic memory 가이드). 루트는 항상 로드되므로 가장 짧게 유지한다. -->

## 문서 지도

| 문서 | 언제 읽히나 | 내용 |
|---|---|---|
| `CLAUDE.md` (이 파일) | 항상 | 절대 규칙, 용어, Git, Claude 규칙 |
| `backend/CLAUDE.md` | backend 파일을 열 때 | 구조, API·DB 규약, 테스트 |
| `frontend/CLAUDE.md` | frontend 파일을 열 때 | 구조, 상태관리, 온디바이스 |
| `backend/domains/<도메인>/CLAUDE.md` | 그 도메인을 열 때 | 파일 책임, 도메인 규칙 |
| `docs/테크스펙.md` | 요구사항 확인이 필요할 때 | **FR/NFR 원본. 충돌 시 이쪽이 기준** |
| `docs/open-questions.md` | 합의 전 항목 확인 | 미정 사항 체크리스트 |
| `docs/doc-management.md` | **문서를 새로 만들거나 정리할 때** | 문서 관리 원칙, 절차, 파일 구조 |
| `.claude/skills/*/SKILL.md` | 그 스킬을 호출할 때 | 브랜치·커밋·PR 규약 |

**권위 순서: 테크스펙(FR/NFR) > 이 문서 > 하위 CLAUDE.md.** 이 문서의 하드룰은 전부 NFR에서 나옵니다. 둘이 어긋나면 테크스펙이 맞고, 이 문서를 고칩니다.

---

## 절대 규칙 (Hard Rules)

> 이 4개는 **위반 시 법적 리스크 또는 서비스 신뢰 붕괴**로 이어집니다. PR에서 발견되면 다른 리뷰 의견과 무관하게 머지 불가.

- **H-1** 미승인 초안은 검수 권한이 있는 교사의 저장·조회·수정을 허용합니다. 학부모 공개와 외부 공유·복사·전송은 교사 승인(`status == approved`)과 대상 원아 접근 권한 검사를 통과해야 하며, 공개 경로는 단일 게이트 함수로 모읍니다. 검수용 초안 보관과 승인본 공개를 구분합니다.
- **H-2** 외부 LLM에 **실명·생년월일·학부모 정보·얼굴 임베딩·실명↔토큰 매핑을 보내지 않습니다.** 실명은 `CHILD_A` 토큰으로 치환합니다. 보낼 수 있는 것은 비식별 텍스트와 **동의 원아의** 활동 사진·영상 프레임뿐이고, 교사가 업로드 대상으로 확정하고 **귀속이 끝난 것만** 씁니다 — **미동의 원아가 귀속된 사진·미식별 얼굴이 남은 사진은 제외합니다.** 귀속·동의 판정 방법과 프레임 수 상한은 `backend/domains/agents/CLAUDE.md`.
- **H-3** 얼굴 검출·임베딩 계산은 브라우저에서만 하고, **등록용 원본 사진은 서버·S3로 보내지 않습니다** — 벡터를 추출한 뒤 브라우저에서 폐기합니다. 활동 사진·영상은 별도 미디어 흐름으로 **교사가 업로드 대상으로 확정한 것만** S3에 올립니다(업로드 자체는 필수 동의(②) 범위). **미동의 원아 보호는 업로드 차단이 아니라 LLM 경로 제외(H-2)로 합니다.** 로컬 검수·확정 상태의 세부는 `frontend/CLAUDE.md` §온디바이스.
- **H-4** 열람은 `AccessLog`, 파기는 `DeletionLog`에. 반대로 stdout 로그에 실명·연락처·토큰·임베딩 값을 찍지 않습니다.

<!-- 경위: 얼굴 블러는 09/13 폐기(스파이크 결과 인식은 되나 블러 영역이 과도하게 큼). 영상·음성은 09/14로 로컬 처리 대상에서 빠져 "확정" 상태 자체가 없다. 현행 규칙과 이유는 frontend/CLAUDE.md §온디바이스, backend/domains/media/CLAUDE.md에 있다. -->

## 도메인 용어

**한 가지 단어만 씁니다.** 새 용어가 필요하면 이 표에 추가한 뒤 사용합니다.

| 의미 | 코드에서 쓰는 말 | 쓰지 않는 말 |
|---|---|---|
| 원아 | `child` | kid, student, baby |
| 어린이집(원) | `center` | kindergarten, daycare, organization(도메인 이름과 구분) |
| 어린이집 가입 코드 | `center_code` | center_id(PK와 혼동), 기관코드, 어린이집 아이디 |
| 반 | `class_` / `klass` | group, room |
| 관찰일지 | `observation_log` | journal, diary |
| 알림장 | `parent_note` | notice(공지와 혼동), letter |
| 공지 | `notice` | announcement, board |
| 근거 | `evidence` | source, proof |
| 미분류함 | `unclassified` | pending, unknown |
| 승인 | `approve` / `approved` | confirm, publish(노출과 구분) |

---

## Git

- **브랜치·커밋·PR을 만들 때는 먼저 `/create-branch` `/commit` `/pr` 스킬을 호출합니다. 배포·CI를 건드릴 때는 `/deploy`를 호출합니다.** 명명 규칙, 커밋 메시지 형식, PR 규약이 전부 그 안에 있습니다. 규약을 기억에서 꺼내 쓰지 마세요.
- **develop에 직접 push하지 않습니다.** 문서 오타 수정 등 `[fix]` 급만 예외 (`docs/doc-management.md`).
- **원아 사진·영상·음성을 테스트 픽스처로 커밋하지 않습니다.** 합성 데이터나 팀원 본인 사진을 씁니다.
- 코드·식별자·커밋 제목은 **영어**, PR 본문·주석·문서는 **한국어**. 파트별 예외는 해당 `CLAUDE.md`에 있습니다.

---

## 환경변수

- 코드에 URL·키·비밀번호를 박지 않습니다. `.env.example`에는 **키 이름과 설명만** 커밋합니다.
- 키가 커밋됐다면 되돌리는 것으로 끝내지 말고 **즉시 폐기·재발급**합니다.

---

## Claude 작업 규칙

- 코드를 쓰기 전에 §절대 규칙과 해당 파트 `CLAUDE.md`를 확인하고, 변경 이유를 요구사항 ID(`FR-05` 등)로 답니다.
- **스펙에 근거가 없는 값은 상상해서 채우지 않고 "미정"으로 남기고 질문합니다.**
- **스펙(요구사항 ID, 데이터 모델, 인터페이스)이 바뀌면 `docs/테크스펙.md`를 먼저 고칩니다. 코드가 스펙을 앞서가면 안 됩니다.**
- **대화 중 규칙을 바꾸자는 이야기가 나오면 문서를 바로 고치지 않습니다.** `docs/open-questions.md`에 제안으로 올리고, **디스코드 채널에도 공유하라고 질문자에게 말합니다.** `[rule]`은 팀 승인 대상입니다 (`docs/doc-management.md`).
- **결정을 문서에 반영하기 전에 "이거 팀에서 논의된 사항인가요?"를 먼저 묻습니다.** "논의됐다"는 답을 받은 것만 반영하고, 답을 못 받았으면 채팅으로만 말하고 문서는 건드리지 않습니다. `docs/open-questions.md`의 체크박스를 닫는 것도 같습니다 — 닫는 순간 다음 사람이 그걸 근거로 코드를 씁니다.
  - **Claude의 추천을 대화 상대 한 명이 승인한 것은 "논의된 사항"이 아닙니다.** 대화에서 합의된 것처럼 보여도 그 자리에 없던 팀원에게는 통보된 적이 없습니다. 확정과 비확정을 구분해 적는 것이 회의 정리 원칙입니다 (`docs/doc-management.md` §원칙 ③).
- 먼저 확인받을 것: 의존성 추가·삭제, 디렉터리 구조 변경, DB 스키마 변경, 마이그레이션 생성, 새 패턴 도입
- 하지 않기: 승인·권한 검사 우회(H-1) / 동의 확인 우회(H-3) / 실명·연락처 삽입(H-2) / 테스트 약화·스킵 / 요청 범위를 넘는 리팩터링(**보고만** 합니다)
- 코드 변경을 마치면 **해당 파트 `CLAUDE.md`에 적힌 린터·포매터**를 돌리고 결과를 보고합니다. 설치돼 있지 않으면 재시도하지 말고 그 사실을 보고합니다.

---

## 이 문서를 고칠 때

`docs/doc-management.md`를 따릅니다 — 반영 순서, `[fix]`/`[rule]` 등급, 이유 남기는 법, 전파 방법이 전부 거기 있습니다.

> **계속 지켜야 할 규칙은 채팅으로 말하지 말고 문서에 적으세요.** 대화로만 준 지시는 컨텍스트가 압축되면 사라집니다.
